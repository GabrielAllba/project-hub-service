# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# server.port=${SERVER_PORT:8081}

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# grpc.auth-service.host=${GRPC_AUTH_SERVICE_HOST:authentication-service}
# grpc.auth-service.port=${GRPC_AUTH_SERVICE_PORT:50051}

spring.datasource.url=jdbc:postgresql://localhost:5432/project-hub-db
spring.datasource.username=postgres
spring.datasource.password=12345
server.port=8081

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

grpc.auth-service.host=localhost
grpc.auth-service.port=50051